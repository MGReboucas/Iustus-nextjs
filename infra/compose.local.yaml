# Apenas PostgreSQL local. Não é configuração de produção.
name: iustus-local
services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: iustus
      POSTGRES_USER: iustus
      POSTGRES_PASSWORD: iustus-local-only
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U iustus -d iustus"]
      interval: 5s
      timeout: 5s
      retries: 10
volumes:
  postgres_data:
